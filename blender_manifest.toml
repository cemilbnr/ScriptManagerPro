schema_version = "1.0.0"

id = "script_manager_pro"
version = "1.0.0"
name = "Script Manager Pro"
tagline = "Organize, edit and run external Python scripts from disk"
maintainer = "Cemil Berk"
type = "add-on"

tags = ["Development"]

blender_version_min = "4.4.0"

license = [
  "SPDX:GPL-3.0-or-later",
]

[permissions]
files = "Read, run and save scripts in the selected library folder"
